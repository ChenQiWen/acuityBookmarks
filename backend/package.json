{
  "name": "acuity-bookmarks-backend",
  "version": "1.0.0",
  "description": "Backend for AcuityBookmarks - Cloudflare Worker",
  "type": "module",
  "main": "cloudflare-worker.js",
  "scripts": {
    "dev": "bunx wrangler dev --remote --port 8787 # 默认连接远程 Cloudflare 服务进行开发",
    "deploy": "bunx wrangler deploy # 部署 Cloudflare Worker 到平台",
    "lint": "eslint . --cache # 运行 ESLint（带缓存）",
    "lint:fix": "eslint . --cache --fix # ESLint 自动修复",
    "lint:check": "eslint . --cache || echo '⚠️  ESLint found issues but continuing...' # 严格检查但不中断",
    "audit": "bun audit # 依赖安全审计",
    "health-check": "curl -f http://localhost:8787/api/health || exit 1 # 本地健康检查（wrangler dev）"
  },
  "dependencies": {
    "uuid": "^11.1.0",
    "zod": "^3.22.4"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "devDependencies": {},
  "runtime": "bun",
  "keywords": [
    "cloudflare-worker",
    "bookmark",
    "chrome-extension",
    "high-performance"
  ]
}
