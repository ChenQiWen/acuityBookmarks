/**
 * DEPRECATED: 此文件已被统一搜索实现取代。
 *
 * 迁移说明：
 * - 请使用 `@/core/search/strategies/fuse-strategy` 中的 FuseSearchStrategy
 * - 或统一入口 `@/application/search/search-app-service` 的 `searchAppService.search(query, { strategy: 'fuse' | 'hybrid' })`
 *
 * 保留该文件仅为避免并行分支的大改动冲突；不要再新增任何实现。
 */

export {}