/**
 * DEPRECATED: legacy Hybrid Search Engine
 *
 * 请改用统一入口：
 *   import { searchAppService } from '@/application/search/search-app-service'
 *   await searchAppService.search(query, { strategy: 'hybrid' | 'fuse', limit })
 */

export {}
