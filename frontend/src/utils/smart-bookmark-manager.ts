/**
 * @deprecated 该管理器属于早期 utils 层的业务编排封装。
 * 请改用应用层封装：`application/bookmark/smart-bookmark-manager.ts` 或直接使用 `bookmarkChangeAppService`。
 */
export { SmartBookmarkManager, smartBookmarkManager, type ApplyChangesOptions, type ApplyChangesResult } from '@/application/bookmark/smart-bookmark-manager'
