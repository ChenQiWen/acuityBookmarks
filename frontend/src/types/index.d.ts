/**
 * 全局类型定义统一导出
 *
 * 这是项目类型定义的统一入口点，包含所有类型定义的导出
 *
 * @example
 * ```typescript
 * // 导入核心类型
 * import type { Result, Optional } from '@/types'
 *
 * // 导入领域类型
 * import type { BookmarkNode, SearchOptions } from '@/types'
 *
 * // 导入应用层类型
 * import type { UserInfo, Notification } from '@/types'
 * ```
 */

// ============================================================================
// 核心类型 (Core Types)
// ============================================================================
// 通用类型、Result 模式、错误处理
export * from './core'

// ============================================================================
// 领域类型 (Domain Types)
// ============================================================================
// 书签、搜索、清理等业务领域类型
export * from './domain'

// ============================================================================
// 应用层类型 (Application Types)
// ============================================================================
// 认证、通知等应用服务类型
export * from './application'

// ============================================================================
// 基础设施类型 (Infrastructure Types)
// ============================================================================
// IndexedDB、HTTP 等基础设施类型
export * from './infrastructure'

// ============================================================================
// UI 层类型 (UI Types)
// ============================================================================
// Store、组件等 UI 相关类型
export * from './ui'
