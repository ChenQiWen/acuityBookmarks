# 开发环境配置
# ⚠️ 重要：VITE_API_BASE_URL 和 VITE_CLOUDFLARE_WORKER_URL 由 watch-build.js 自动管理
# 请勿在此文件中手动设置这两个变量，否则可能导致 HTTP/HTTPS 冲突
# watch-build.js 会根据后端运行模式自动设置正确的 HTTPS URL

# 其他配置
VITE_CLOUDFLARE_MODE="true"
