# ============================================
# AcuityBookmarks 统一环境变量配置
# ============================================
# 
# 📝 说明：
# - 此文件包含所有非敏感的环境变量配置
# - 可以安全地提交到版本控制
# - 敏感信息（如 API 密钥）应放在 .env.local 中（不提交）
#
# 🔄 Vite 环境变量加载优先级：
# 1. .env.local（最高优先级，gitignore，本地覆盖）
# 2. .env.development / .env.production（根据 NODE_ENV，已整合到此文件）
# 3. .env（基础配置，最低优先级）
#
# ============================================
# 通用配置（所有环境）
# ============================================

# Cloudflare 模式标识
# 设置为 "true" 表示使用 Cloudflare Worker
VITE_CLOUDFLARE_MODE="true"

# ============================================
# 生产环境配置（仅在 NODE_ENV=production 时生效）
# ============================================
# 注意：开发环境的 API URL 由 watch-build.js 自动管理
# 请勿在此文件中手动设置开发环境的 VITE_API_BASE_URL

# API 基础 URL（生产环境）
# 优先使用自定义域名（推荐绑定到 api 子域）
VITE_API_BASE_URL="https://api.acuitybookmarks.com"

# Cloudflare Worker URL（备用）
# 如果自定义域名不可用，可以切换到 workers.dev
VITE_CLOUDFLARE_WORKER_URL="https://acuitybookmarks.cqw547847.workers.dev"

# ============================================
# 注意事项
# ============================================
#
# ⚠️ 开发环境的 API URL 由 watch-build.js 自动管理
# 请勿在 .env 文件中手动设置开发环境的 VITE_API_BASE_URL
# watch-build.js 会根据后端运行模式自动设置正确的 HTTPS URL
#
# 🔒 敏感信息（如 Supabase 密钥）请放在 .env.local 文件中
# .env.local 文件已被 gitignore，不会提交到版本控制
